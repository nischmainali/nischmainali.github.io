[build]
  command = "python3 scripts/check_content.py --source-only && hugo"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.164.0"
  HUGO_ENV = "production"

[context.production.environment]
  HUGO_ENABLEGITINFO = "true"
